package com.streams.examples.mystreamsexamples;

import com.streams.examples.mystreamsexamples.service.StreamService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MyStreamsExamplesApplication {

	public static void main(String[] args) {
		SpringApplication.run(MyStreamsExamplesApplication.class, args);
	}

}
